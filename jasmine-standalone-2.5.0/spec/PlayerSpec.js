describe("Set", function() {
  var set;

  beforeEach(function() {
    set = new Set();
  });

  it("should be defined", function() {
    //demonstrates use of custom matcher
    expect(typeof set).toBe('object');
  });

  it('should be able to contain', function() {

  })
 
});
